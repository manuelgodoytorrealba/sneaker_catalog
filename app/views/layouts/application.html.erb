<!DOCTYPE html>
<html>
  <head>
    <title>SneakersCatalog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="bg-gray-50 text-gray-900">
    <header class="bg-white border-b">
      <div class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between">
        <h1 class="text-xl font-bold"><%= link_to "Catálogo de Zapatillas", root_path %></h1>
        <%= link_to "Nueva sneaker", new_sneaker_path, class: "px-3 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-500" %>
      </div>
    </header>

    <main class="max-w-7xl mx-auto px-4 py-8">
      <%= yield %>
    </main>
  </body>
</html>